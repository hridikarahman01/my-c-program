#include<stdio.h>
main()
{
    int n,i,m=0;
    printf("Input number of terms:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        if(m=i*i*i);
        printf("\nThe numbers are %d and cube of the numbers %d : %d",i,i,m);
    }
}

