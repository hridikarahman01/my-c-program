#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,r;
    printf("Enter a number to reverse\n");
    scanf("%d", &n);
    while(n!=0)
    {
        a = n * 10;
        b = n + n%10;
        c = n/10;
    }
    printf("Reverse is : %d",r);
    return 0;
}
